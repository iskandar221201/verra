package conversation

// Router is kept minimal since service.go already handles routing internally.
// This file exists to satisfy the folder structure requirement.
// The routing logic (AI mode vs human mode) is implemented in Service.route().
